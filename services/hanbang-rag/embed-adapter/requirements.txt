# embed-adapter dependencies
# torch is installed separately in Dockerfile with CPU-only index to prevent
# the CUDA variant from being pulled in.  Do NOT add torch here.

fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
sentence-transformers>=3.0.0,<4.0

# pydantic is pulled in by fastapi; no separate pin needed.

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0
