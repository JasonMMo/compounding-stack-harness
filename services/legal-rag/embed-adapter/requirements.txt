# embed-adapter dependencies
# Pins mirror the legal-rag app requirements.txt where versions overlap.

fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
httpx>=0.27.0,<1.0

# pydantic is pulled in by fastapi; no separate pin needed.

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
respx>=0.21.0,<1.0
