# frontend/adapters/vanilla-htmx — Python dependencies
#
# Flask: lightweight WSGI server + Jinja2 templating (zero heavy deps).
# PyYAML: YAML parser for contract_loader.py (G-1: runtime contract reading).
# pytest: L1 unit test runner (same as repo-wide harness).
#
# Install: pip install -r requirements.txt
# Python: 3.11+

flask>=3.0,<4.0
PyYAML>=6.0,<7.0
pytest>=7.0,<9.0
