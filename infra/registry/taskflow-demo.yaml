# infra/registry/taskflow-demo.yaml
# 고객/프로필: taskflow-demo (태스크플로우 도메인 preview 데모)
# Growth-35 DevOps loop — preview tier, in-memory store, fake data only
# 시크릿 평문 절대 금지: secret_ref 볼트 키 이름만
slug: taskflow-demo
contact:
  channel: internal             # 내부 데모 — 실 고객 아님
  handle: "demo"
preview:
  subdomain: taskflow-demo.n9n.co.kr
  url: https://taskflow-demo.n9n.co.kr
  coolify_project: tmxqpuzxk4uywv67bhywnnty
  coolify_app: q1378vp78qvwfv5hh5hzv80o
  coolify_env: null
  coolify_privkey_uuid: s127pafarr46wlu1r2mre2te  # 재사용 (기존 preview 키)
  compose_file: deploy/preview/taskflow-demo.compose.yml
  manifest_server_path: null    # 없음 — 매니페스트는 seeder 컨테이너가 shared volume에 생성
  status: live
  deployed_at: "2026-06-24"
  build_commit: null
  tls: null
production:
  install_method: null
  installed_at: null
  stack:
    frontend: vanilla-htmx
    backend: fastapi
secret_ref:
  - vault/taskflow-demo/secret-key    # infra/secrets/taskflow-demo-secret-key.txt (gitignored)
cost_note: "preview 티어 공유 VPS (187.77.140.157) 귀속분, Hostinger KVM2 $8.99/월 공유. Growth-125: embed 사이드카(multilingual-e5-base) 추가 — 런타임 RAM +~400-500MB. legal-rag 도 별도 embed 가동 중이라 8GB 공유 여유 모니터링 필요 (Coolify 프로젝트 net 격리로 embed 공유 불가). hedge 후보: 단일 공유 embed 서비스."
data_note: "in-memory store — 재시작 시 데이터 소멸. 실 고객 데이터 저장 금지."
feature_note: "Lite-AI 유사검색(wire project.search-similar): TASKFLOW_EMBED_URL=http://embed:8080 설정 시 mode=semantic('AI 의미검색' 배지), 미설정/미가동 시 lexical 폴백('키워드 검색'). embed-adapter 규약(/embed query head · /embed/batch passage head, G-87) 재사용 (Growth-125)."
