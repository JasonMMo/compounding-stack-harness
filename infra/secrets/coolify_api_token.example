Copy this to infra/secrets/coolify_api_token and replace the whole content with ONLY the token value (one line, no COOLIFY_API_TOKEN=, no quotes). Coolify token looks like: 2|abcdef... (the leading id| is part of it, keep it). 그 파일은 gitignored. Claude 는 tr 로 읽고 길이만 확인 (값 비출력).
